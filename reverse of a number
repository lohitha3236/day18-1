#include<stdio.h>
int main()
{
int n,revrse=0,rem;
printf("enter the number");
sacnf("%d",&n);
while(n!=0)
{
rem=n%10;
reverse=reverse*10+rem;
n/=10;
}
printf("reversed number:%d",reverse);
return 0;
}
